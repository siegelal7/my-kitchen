import React from 'react';
import {View, InputField} from 'react-native';
import Input from '../components/Input';

const Login = () => {
  return (
    <View>
      <Input label="email" value={} />
    </View>
  );
};

export default Login;
